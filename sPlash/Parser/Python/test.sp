
test1: String ();

Var1 : T;

max:Int (a:Int, b:Int) {
  if a > b {
    return a;
  }
  return b;
}

pi:Int = +3 || "Thiss is way to complitcated" && Hi + f(123, 1231);

main : Int () {
    var : String = "Hello World!";
    return 0;
}